const PENDING = 0,
      CORRECT = 1,
      INCORRECT = 2;

const PLAYING = 0,
      RESULTS = 1;

export default 0;

export {
    PENDING,
    CORRECT,
    INCORRECT,
    PLAYING,
    RESULTS
};
